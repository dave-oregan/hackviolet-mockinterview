# hackviolet-mockinterview
Use this command for docker setup -> docker compose up --build

##
The final commit was to move the code used in the submission of the project from its side branch to main.
